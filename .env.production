# Production env for static hosting builds (GitHub Actions picks this up).
# These two values are PUBLISHABLE by design: they are shipped to every
# visitor's browser in the app bundle. Data protection comes from Row Level
# Security on the database, not from keeping these private.
# NEVER put real secrets here (service-role keys, Supabase access tokens,
# passwords) — only VITE_ publishable values belong in git.
VITE_SUPABASE_URL=https://enerawynpulnysufrrah.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_DZ-KMIcNh4QveEmjLjvBQQ_AO3qXovJ
