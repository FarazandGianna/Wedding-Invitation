[TEMPLATE]
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Optional: open "/" directly on a single-invitation deployment
#VITE_DEFAULT_INVITE_SLUG=your-slug

# Local tooling only (gitignored .env.local) — never used by the frontend:
#TEST_INVITE_SLUG=sample-wedding
#SUPABASE_SERVICE_ROLE_KEY=server-side-only
